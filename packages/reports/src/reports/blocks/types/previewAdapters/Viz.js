
/** @type {import(".").BlockPreviewAdapterFunction} */
const vizPreviewAdapter = props => ({content: props});

// todo1.0 - change once the Viz component has been rewritten

export default vizPreviewAdapter;
